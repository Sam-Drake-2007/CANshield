export default function LandingPage() {
    return (
        <main>
            {/* WRITE THE CSS DIRECTLY IN the 'className' attribute, look up the equivalent CSS style on TailwindCSS' site if needed */}
            <h1 className="text-red-500 font-bold text-2xl">This is the landing page.</h1>
            <p className="mt-5 text-blue-400 text-lg">This is another piece of text.</p>
            <p>The only 2 other routes </p>
        </main>
    );
}